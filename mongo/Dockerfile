FROM mongo:latest

# MongoDB authentication environment variables
# These are standard MongoDB Docker environment variables
# Set them at runtime using docker run -e or in docker-compose
ENV MONGO_INITDB_ROOT_USERNAME=""
ENV MONGO_INITDB_ROOT_PASSWORD=""

CMD ["mongod"]

EXPOSE 27017
EXPOSE 28017